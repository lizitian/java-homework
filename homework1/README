3.4-3.18

猜单词：给定单词数组（不少于10个），计算机随机选择其中的一个，并显示单词长度。用户每次猜一个字母，如正确，将在相应的位置显示出来，重复该过程，直至字母全部猜出。用户也可以通过输入数字0来请求提示，此时计算机会随机选择一个尚未猜出的字母显示出来。

例如随机单词为apple，计算机先打印_____
*用户输入a，计算机显示a____
*用户输入e，计算机显示a___e
*用户输入0，计算机显示app_e（也可能是a__le）
